interface:
  display_name: "Codex CTF Orchestrator"
  short_description: "Authorized CTF toolchain development, evaluation, and challenge routing"
  default_prompt: "Use $codex-ctf-orchestrator. Select TOOLCHAIN for owned CTF/DFIR tool development, EVAL for synthetic or public-historical capability testing, and CHALLENGE only for a specific explicitly authorized challenge. Propagate the complete scope contract to every leaf, preserve durable checkpoints, and never evade safety controls."
policy:
  allow_implicit_invocation: true
